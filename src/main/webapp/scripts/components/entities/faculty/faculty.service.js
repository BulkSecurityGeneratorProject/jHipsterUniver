'use strict';

angular.module('univerApp')
    .factory('Faculty', function ($resource, DateUtils) {
        return $resource('api/facultys/:id', {}, {
            'query': { method: 'GET', isArray: true},
            'get': {
                method: 'GET',
                transformResponse: function (data) {
                    data = angular.fromJson(data);
                    return data;
                }
            },
            'update': { method:'PUT' }
        });
    });
